class Solution {
    public void reverseString(char[] s) {
        char temp;
        int size = s.length;
        for (int i = 0; i < size; i++) {
            temp = s[i];
            s[i] = s[size-1];
            s[size-1] = temp;
            size--;            
        }
    }
}
